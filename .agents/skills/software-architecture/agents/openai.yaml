interface:
  display_name: "Software Architecture"
  short_description: "Architecture guidance for litestar-pagination."
  default_prompt: "Use $software-architecture to design or review cursor core, optional adapters, public APIs, and Litestar integration boundaries."

policy:
  allow_implicit_invocation: true
